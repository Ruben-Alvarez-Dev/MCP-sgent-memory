# I01 — Identity/session change package

Date: 2026-07-13

Machine: `jart-os-workstation`

Branch: `change/jart-memory-identity-core`

Base commit: `f2a2b3e33cba40365b032adacc7f0f8ebfc8607a`

## Scope

Created the approved proposal, design, plan, test plan, and three implementation
spec deltas before behavior code.

## Red evidence

The normalized-document test was generalized from one hard-coded change to every
OpenSpec change. The targeted run failed because the new spec deltas had no
frontmatter:

```text
1 failed
AssertionError: openspec/changes/implement-identity-session-core/
specs/identity-context/spec.md does not start with frontmatter
```

## Green evidence

After adding normalized unique frontmatter to all three deltas:

```text
./tools/check-foundation.sh
6 passed
Ruff: all checks passed
Ruff format: 1 file already formatted
```

Independent document hooks returned `pass` with zero errors for both proposal and
design. `git diff --check` exited 0.

No behavior implementation, legacy handler, production data, or external service
was touched in this iteration.
