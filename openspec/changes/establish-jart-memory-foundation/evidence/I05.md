# I05 — Frozen foundation gate

Date: 2026-07-13

Machine: `jart-os-workstation`

Branch: `change/jart-memory-foundation`

Base commit: `f934a7c332a4cf37e322322fe66af86faab88294`

## Scope

Added a direct development dependency for JSON Schema validation, a complete
`uv.lock`, a versioned foundation validation entrypoint, and governance-as-code
tests for the lock and gate. No runtime behavior changed.

## Red evidence

Command:

```text
python -m pytest tests/contracts -q
```

Result:

```text
5 passed, 1 failed in 0.10s
AssertionError: the Python dependency graph must be committed
```

The failure represented the missing reproducibility control.

## Green evidence

The lock resolved 40 package records for CPython 3.12.13. A clean repository-local
environment installed 38 selected packages. The exact gate commands returned:

```text
uv sync --frozen --extra dev: exit 0
uv run --frozen --extra dev pytest tests/contracts -q: 6 passed
uv run --frozen --extra dev ruff check tests/contracts: passed
uv run --frozen --extra dev ruff format --check tests/contracts: 1 file already formatted
```

`uv.lock` SHA-256:

```text
faaac0ca8f94afad4a74bb39f6da3f7fdd90bf10bb31da94b5580a6fc81dee58
```

The entrypoint requires uv `0.11.28` and uses the committed lock in frozen mode.
It can be called locally and by any later CI provider without duplicating commands.

`tools/check-foundation.sh` SHA-256:

```text
5772f9b80618ddd0975ea9a71d0cc421b8c96c92f646a86d1931b755e5c2bcf1
```

## Independent verification pending

Hosted CI execution is not part of this iteration. A local green run is not
represented as hosted-CI evidence.
