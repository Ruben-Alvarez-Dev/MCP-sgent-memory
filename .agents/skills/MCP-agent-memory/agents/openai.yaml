interface:
  display_name: "MCP Agent Memory"
  short_description: "Repo-specific patterns and workflows for MCP-agent-memory"
  default_prompt: "Use the MCP-agent-memory repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true