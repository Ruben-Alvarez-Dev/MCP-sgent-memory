Eres ingeniero MCP senior. cwd=/Users/manu/.mcp-agent-memory. NO uses git. No toques ficheros fuera de tu propiedad. No loguees tokens.

PROPIEDAD EXCLUSIVA: src/L2_conversations/server/main.py, src/L3_facts/server/main.py, src/L0_capture/server/main.py, src/unified/server/main.py. NO toques L5 ni identity.py ni tests.

CONTEXTO: src/shared/identity.py YA EXISTE y NO se modifica. API: bind_identity() devuelve Identity con .assert_agent(x) que devuelve el scope efectivo o lanza ScopeError, y .as_dict(). En modo strict lanza IdentityError en el arranque (fail-closed). REFERENCIA del patrón: mira src/L5_routing/server/main.py y busca las lineas con IDENTITY.

TAREA: replicar el patron de identidad:
1. En cada uno de tus 4 servidores, tras la construccion de config/store/db, anyade:
   from shared.identity import bind_identity
   IDENTITY = bind_identity()  # M4: strict mode raises here (fail-closed boot, ISO-14)
2. L2_conversations: en save_conversation, search_conversations y list_threads (verifica nombres reales de params con grep), PRIMERA linea del body:
   agent_scope = IDENTITY.assert_agent(agent_scope)
3. unified/server/main.py: bind tras config; en health_check() anyade checks["identity"] = IDENTITY.as_dict(). No cambies el loop de registro de modulos.
4. L3_facts y L0_capture: SOLO el bind (sus tools usan user_id, no agent-scope; M4 no liga user_id).
5. VERIFICA: importa los 4 modulos con importlib (nombres unicos) sin error, y corre:
   .venv/bin/python -m pytest tests/core/test_mcp_modules.py tests/app -q
6. REPORTA al final: cambios por fichero, resultado pytest, desviaciones.
