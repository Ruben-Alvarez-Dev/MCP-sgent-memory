---
id: EVIDENCE-implement-identity-session-core-I03
title: Session lifecycle red-green evidence
type: evidence
status: recorded
version: 0.1.0
owners: [identity, memory, testing]
deciders: [ruben]
created_at: 2026-07-13
last_verified_at: 2026-07-13
authority: CHANGE-implement-identity-session-core
supersedes: []
superseded_by: null
related_changes: [implement-identity-session-core]
---

# I03 — Session lifecycle

## Red

Command:

```text
uv run --frozen --extra dev pytest tests/unit/domain/test_session.py -q
```

Result: collection failed with `ModuleNotFoundError: No module named
'jart_memory.domain.session'`. This is the expected Red state because the
session domain module was absent before implementation; no assertion was
weakened and no test was skipped.

## Scope

The tests cover immutable session values, `active -> ending -> ended`,
`active|ending -> revoked`, typed illegal-transition rejection, active-only
sequence advancement, monotonic high-watermark replacement, and typed stale
writer rejection without mutation.

## Green

Commands:

```text
uv run --frozen --extra dev pytest tests/unit/domain/test_session.py -q
13 passed in 0.01s

uv run --frozen --extra dev ruff check src/jart_memory tests/unit/domain/test_session.py
All checks passed!

uv run --frozen --extra dev ruff format src/jart_memory tests/unit/domain/test_session.py
1 file reformatted, 4 files left unchanged

uv run --frozen --extra dev ruff format --check src/jart_memory tests/unit/domain/test_session.py
5 files already formatted

git diff --check
exit 0

./tools/check-foundation.sh
Checked 38 packages in 0.90ms
6 passed in 0.08s
All checks passed!
```

Result: Green. The immutable session lifecycle and optimistic sequence behavior
pass the targeted suite and the frozen foundation gate. No production store,
legacy handler, model, deployment, or real user data was touched.
