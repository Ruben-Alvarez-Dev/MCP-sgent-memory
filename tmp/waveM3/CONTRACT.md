# Contexto M3 (cwd=/Users/manu/.mcp-agent-memory)
Motor: src/shared/memory_db.py — MemoryDB.search(vector, limit, score_threshold, filter, sparse_query=None, sparse_weight=0.3).
Filtros scope OBLIGATORIOS (fail-closed). Embeddings: hash_vector(text, dim) en shared.memory_db es determinista.
PROHIBIDO: git, tocar ficheros fuera de tu propiedad, puertos/daemons, modelos locales.
Testear: .venv/bin/python -m pytest <tu-test> -q (NUNCA suite completa). Ruff limpio en ficheros nuevos.
