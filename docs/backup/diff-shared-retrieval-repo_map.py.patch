--- MCP-servers/shared/retrieval/repo_map.py	2026-04-20 21:22:56
+++ src/shared/retrieval/repo_map.py	2026-04-20 21:22:58
@@ -1,182 +1 @@
-"""Repository map helpers for hierarchical context lookups."""
-
-from __future__ import annotations
-
-import ast
-import re
-from pathlib import Path
-
-from shared.models.repo import RepoMap, RepoNode
-
-
-_IMPORT_RE = re.compile(
-    r"""^\s*(?:from\s+([A-Za-z0-9_./]+)\s+import|import\s+([A-Za-z0-9_./]+))""",
-    re.MULTILINE,
-)
-_TS_IMPORT_RE = re.compile(r"""from\s+['"](.+?)['"]""")
-
-
-def _project_root(project_root: str | None = None) -> Path:
-    if project_root:
-        return Path(project_root).resolve()
-    return Path.cwd().resolve()
-
-
-def _safe_rel(path: Path, root: Path) -> str:
-    try:
-        return str(path.resolve().relative_to(root))
-    except ValueError:
-        return str(path.resolve())
-
-
-def _find_target(query: str, root: Path) -> Path | None:
-    candidate = (root / query).resolve()
-    if candidate.exists():
-        return candidate
-
-    query_name = Path(query).stem
-    matches = [p for p in root.rglob("*") if p.is_file() and p.stem == query_name]
-    if len(matches) == 1:
-        return matches[0].resolve()
-    return None
-
-
-def _python_file_node(path: Path, root: Path) -> RepoNode:
-    text = path.read_text(encoding="utf-8")
-    tree = ast.parse(text)
-    deps: list[str] = []
-    children: list[RepoNode] = []
-
-    for node in tree.body:
-        if isinstance(node, ast.Import):
-            for alias in node.names:
-                deps.append(alias.name)
-        elif isinstance(node, ast.ImportFrom) and node.module:
-            deps.append(node.module)
-        elif isinstance(node, ast.ClassDef):
-            children.append(
-                RepoNode(
-                    path=_safe_rel(path, root),
-                    type="class",
-                    signature=f"class {node.name}",
-                    dependencies=[],
-                )
-            )
-        elif isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
-            children.append(
-                RepoNode(
-                    path=_safe_rel(path, root),
-                    type="function",
-                    signature=f"def {node.name}()",
-                    dependencies=[],
-                )
-            )
-
-    return RepoNode(
-        path=_safe_rel(path, root),
-        type="file",
-        signature=f"module {path.stem}",
-        dependencies=sorted(set(deps)),
-        children=children,
-    )
-
-
-def _generic_file_node(path: Path, root: Path) -> RepoNode:
-    text = path.read_text(encoding="utf-8")
-    deps = [m.group(1) or m.group(2) for m in _IMPORT_RE.finditer(text) if m.group(1) or m.group(2)]
-    deps.extend(_TS_IMPORT_RE.findall(text))
-    children: list[RepoNode] = []
-
-    for match in re.finditer(r"^\s*(?:export\s+)?(?:async\s+)?function\s+([A-Za-z_]\w*)", text, re.MULTILINE):
-        children.append(
-            RepoNode(
-                path=_safe_rel(path, root),
-                type="function",
-                signature=f"function {match.group(1)}()",
-                dependencies=[],
-            )
-        )
-    for match in re.finditer(r"^\s*class\s+([A-Za-z_]\w*)", text, re.MULTILINE):
-        children.append(
-            RepoNode(
-                path=_safe_rel(path, root),
-                type="class",
-                signature=f"class {match.group(1)}",
-                dependencies=[],
-            )
-        )
-
-    return RepoNode(
-        path=_safe_rel(path, root),
-        type="file",
-        signature=f"module {path.stem}",
-        dependencies=sorted(set(deps)),
-        children=children,
-    )
-
-
-def _resolve_dependency(dep: str, root: Path) -> Path | None:
-    clean = dep.replace(".", "/").replace("\\", "/").lstrip("/")
-    candidates = [
-        root / clean,
-        root / f"{clean}.py",
-        root / f"{clean}.ts",
-        root / f"{clean}.tsx",
-        root / f"{clean}.js",
-        root / f"{clean}.jsx",
-        root / clean / "__init__.py",
-        root / clean / "index.ts",
-        root / clean / "index.js",
-    ]
-    for candidate in candidates:
-        if candidate.exists() and candidate.is_file():
-            return candidate.resolve()
-    return None
-
-
-def build_repo_map(query: str, project_root: str | None = None) -> RepoMap | None:
-    """Build a hierarchical summary and immediate dependencies for a target."""
-
-    root = _project_root(project_root)
-    target = _find_target(query, root)
-    if target is None or not target.is_file():
-        return None
-
-    if target.suffix == ".py":
-        root_node = _python_file_node(target, root)
-    else:
-        root_node = _generic_file_node(target, root)
-
-    immediate_dependencies: list[RepoNode] = []
-    for dep in root_node.dependencies:
-        dep_path = _resolve_dependency(dep, root)
-        if dep_path is None:
-            continue
-        dep_node = _python_file_node(dep_path, root) if dep_path.suffix == ".py" else _generic_file_node(dep_path, root)
-        immediate_dependencies.append(dep_node)
-
-    child_bits = ", ".join(child.signature for child in root_node.children[:8]) or "no symbols"
-    dep_bits = (
-        ", ".join(f"{node.path} ({node.signature})" for node in immediate_dependencies[:6])
-        or "no resolved deps"
-    )
-    summary = (
-        f"{root_node.path}: {root_node.signature}. "
-        f"Symbols: {child_bits}. "
-        f"Immediate dependencies: {dep_bits}."
-    )
-
-    return RepoMap(
-        root=root_node,
-        immediate_dependencies=immediate_dependencies,
-        summary=summary,
-    )
-
-
-def get_repo_map(query: str, project_root: str | None = None) -> dict | None:
-    """Public dict-friendly helper used by routers and MCP servers."""
-
-    repo_map = build_repo_map(query, project_root)
-    if repo_map is None:
-        return None
-    return repo_map.model_dump(mode="json")
+# Repository map utilities — see shared/retrieval/__init__.py for get_repo_map()
