# I03 — Architecture and governance foundation

Date: 2026-07-13

Machine: `jart-os-workstation`

Branch: `change/jart-memory-foundation`

Base commit: `c16c2c005f813b122f45d00b99e791755eaa252b`

## Scope

Added repository governance, the OpenSpec project/change package, four proposed
ADRs, and the proposed memory threat model. No runtime source or persisted data was
modified.

## Red evidence

Commands:

```text
.jart-governance/hooks/pre-spec-check.sh openspec/changes/establish-jart-memory-foundation/proposal.md
.jart-governance/hooks/pre-analysis-check.sh openspec/changes/establish-jart-memory-foundation/design.md
```

Results:

```text
pre-spec: fail — missing required section: test plan
pre-analysis: fail — missing required sections: problem, options, trade-offs,
acceptance criteria
```

The failure represented missing normative document structure, not broken setup.

## Green evidence

After adding the required sections, the same commands returned:

```text
pre-spec: pass — zero errors
pre-analysis: pass — zero errors
```

An independent YAML parse checked 10 normative documents, all required frontmatter
keys, and 10 unique document IDs:

```text
frontmatter_documents=10 unique_ids=10 status=pass
```

`git diff --check` exited 0. Runtime behavior remains unchanged and is not claimed
as implemented.
