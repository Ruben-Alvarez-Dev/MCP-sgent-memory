# I04 — Proposed capability and schema contracts

Date: 2026-07-13

Machine: `jart-os-workstation`

Branch: `change/jart-memory-foundation`

Base commit: `b9fda5c0faa4975d99233c1ab299e7238c779e7b`

## Scope

Added six proposed capability specifications, seven JSON Schema 2020-12 files,
and contract tests. All fixtures are labeled, deterministic, sanitized, and
TEST-ONLY. No runtime behavior or production data changed.

## Red evidence

Command:

```text
python -m pytest tests/contracts -q
```

Result:

```text
4 failed in 0.10s
foundation schema inventory mismatch
missing: common, identity-context, session, memory-event, memory-record,
memory-grant, promotion-case
```

The tests failed for the intended reason: the required contracts did not exist.

## Green and refactor evidence

After implementing the contracts, the same targeted command returned:

```text
4 passed in 0.07s
```

Ruff then identified formatting drift without lint errors. After the mechanical
format pass, the complete gate returned:

```text
ruff check tests/contracts: passed
ruff format --check tests/contracts: 1 file already formatted
pytest tests/contracts -q: 4 passed in 0.06s
git diff --check: exit 0
```

The contract suite independently checks schema inventory, Draft 2020-12 validity,
closed reference resolution, sanitized valid records, rejection of non-UUIDv7 and
unknown identity fields, and absence of implicit shared/default/current defaults.

## Limitations

Schema validation does not prove runtime authorization, PostgreSQL RLS, Qdrant
filters, NATS isolation, promotion enforcement, or production readiness. Those
remain explicit future gates in `test-plan.md`.
