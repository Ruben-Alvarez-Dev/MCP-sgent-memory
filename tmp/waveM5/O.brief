Eres técnico de documentación. cwd=/Users/manu/.mcp-agent-memory. NO uses git. PROPIEDAD EXCLUSIVA: README.md. NO toques código.

TAREA: refrescar README.md para reflejar la arquitectura M5 real:
1. Lee README.md completo y las secciones de arquitectura de openspec/specs/storage/spec.md (ojo: describe el estado pre-M2 a propósito, úsalo solo como contexto) y los primeros 40 líneas de openspec/changes/M2-storage/GATE_M2.md y openspec/changes/M4-identidad/GATE_M4.md.
2. Arquitectura real HOY: un único data/memory.db (SQLite stdlib, WAL, sin daemons ni puertos); motor MemoryDB con filtros de scope a nivel SQL (agent_scope/user_id/layer, allowlist), zero-vectors prohibidos (NULL + hash-vector determinista), sparse boost RET-05, delete atómico anti-TOCTOU; servers MCP stdio (unified entrypoint) + sidecar HTTP :8890 con token opcional; identidad M4 (MEMORY_AGENT_ID/TOKEN, modos open/strict, registro agents.json 0600, scripts/register_agent.py); scopes con jail FS y namespace 5 niveles c:/p:/a:/s:/u:; trunk merged con aprobación humana+provenance (ISO-16); ranking determinista (SIN micro-LLM, restricción dura); Qdrant ELIMINADO.
3. Sustituye menciones de Qdrant/BGE-M3/qwen como stack activo por la arquitectura real; conserva el historial solo si aparece como contexto legacy claramente marcado.
4. Añade sección corta "Identity & isolation" con el snippet de env del CLI (MEMORY_AGENT_ID/MEMORY_AGENT_TOKEN/MEMORY_IDENTITY_MODE=strict) y una línea sobre data/agents.json (0600, solo hash).
5. NO inventes métricas ni benchmarks; no cambies la licencia ni badges.
VERIFICA: grep -in qdrant README.md → solo menciones legacy marcadas o 0.
REPORTA: secciones cambiadas, resultado del grep.
