---
id: EVIDENCE-implement-identity-session-core-I04
title: Memory isolation red evidence
type: evidence
status: recorded
version: 0.1.0
owners: [memory, security, testing]
deciders: [ruben]
created_at: 2026-07-13
last_verified_at: 2026-07-13
authority: CHANGE-implement-identity-session-core
supersedes: []
superseded_by: null
related_changes: [implement-identity-session-core]
---

# I04 — Memory isolation policy

## Red

Command:

```text
uv run --frozen --extra dev pytest tests/unit/policy/test_isolation.py -q
```

Result: collection failed with `ModuleNotFoundError: No module named
'jart_memory.policy'`. This is the expected Red state because the isolation
policy package was absent before implementation. The test matrix covers
same-owner permit, tenant/user/agent/session/task mismatches, capability and
ceiling checks, inactive contexts, unsupported broader scopes, and non-sensitive
typed denials.

## Green

Commands:

```text
uv run --frozen --extra dev pytest tests/unit/policy/test_isolation.py -q
17 passed in 0.02s

uv run --frozen --extra dev ruff check src/jart_memory tests/unit/domain/test_session.py tests/unit/policy/test_isolation.py
All checks passed!

uv run --frozen --extra dev ruff format --check src/jart_memory tests/unit/domain/test_session.py tests/unit/policy/test_isolation.py
8 files already formatted

git diff --check
exit 0

./tools/check-foundation.sh
Checked 38 packages in 0.99ms
6 passed in 0.11s
All checks passed!
```

Result: Green. The policy supports only `session_private` and `agent_private`,
enforces tenant and private-owner predicates, and returns non-disclosing typed
denials for all unsupported or unauthorized cases. No production data or
infrastructure adapter was used.
